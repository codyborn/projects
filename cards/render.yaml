services:
  - type: web
    name: cards-websocket-server
    runtime: node
    plan: free
    region: oregon
    rootDir: cards
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
